spring:
  application:
    name: task-catalog
  datasource:
    url: jdbc:h2:mem:taskcatalog;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
  h2:
    console:
      enabled: true
server:
  port: 8098
